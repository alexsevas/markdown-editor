# Тест LaTeX формул

## Inline формулы

Формула Эйнштейна: $E = mc^2$

Квадратное уравнение имеет корни: $x = \frac{-b \pm \sqrt{b^2 - 4ac}}{2a}$

Сумма от 1 до n: $\sum_{i=1}^{n} i = \frac{n(n+1)}{2}$

## Display формулы (отдельными блоками)

Интеграл Гаусса:

$$\int_{-\infty}^{\infty} e^{-x^2} dx = \sqrt{\pi}$$

Формула Байеса:

$$P(A|B) = \frac{P(B|A) \cdot P(A)}{P(B)}$$

Матричное уравнение:

$$\begin{bmatrix} a & b \\ c & d \end{bmatrix} \begin{bmatrix} x \\ y \end{bmatrix} = \begin{bmatrix} ax + by \\ cx + dy \end{bmatrix}$$

## Смешанный текст

Теорема Пифагора $a^2 + b^2 = c^2$ применяется в прямоугольных треугольниках.

Производная функции:

$$\frac{d}{dx}(x^n) = nx^{n-1}$$

Греческие буквы: $\alpha, \beta, \gamma, \Delta, \Omega$

## Сложные формулы

Ряд Тейлора:

$$f(x) = f(a) + f'(a)(x-a) + \frac{f''(a)}{2!}(x-a)^2 + \frac{f'''(a)}{3!}(x-a)^3 + \cdots$$

Уравнение Шрёдингера:

$$i\hbar\frac{\partial}{\partial t}\Psi(\mathbf{r},t) = \hat{H}\Psi(\mathbf{r},t)$$
