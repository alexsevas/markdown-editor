v0.0.7
--------
1. Упрощение редактора
- Удалена синтаксическая подсветка Markdown.
- Весь текст отображается моноширинным шрифтом **Consolas 11pt**.
- Внешний вид приведён к стилю обычного блокнота.

2. Синхронизация прокрутки
- Реализована **пропорциональная синхронизация** между редактором (слева) и предпросмотром (справа).
- Добавлена задержка **100 мс** для плавности.
- Используется флаг `sync_scroll_enabled` для предотвращения рекурсии.
- Реализованы методы:
  - `sync_editor_to_preview()` — синхронизация из редактора в предпросмотр.
  - `sync_preview_to_editor()` — заготовка для обратной синхронизации.
- Используется JavaScript в WebView через `runJavaScript()` для управления прокруткой.

3. Номера строк и улучшенный редактор
- Создан класс `LineNumberArea`:
  - Отображает номера строк с помощью `QPainter`.
  - Поддерживает светлую и тёмную темы.
- Создан класс `TextEditWithLineNumbers` (на основе `QPlainTextEdit`):
  - Автоматически управляет областью номеров строк.
  - Подсвечивает текущую строку (цвет зависит от темы).
  - Обрабатывает прокрутку и изменение размера.
- Номера строк:
  - Выравниваются по правому краю.
  - Ширина области адаптируется под количество строк.
  - Используется тот же шрифт (`Consolas 11pt`), что и в основном тексте.

4. Стилизация интерфейса под темы
- **Тёмная тема**:
  - Меню, выпадающие списки и строка состояния: белый текст на тёмно-сером фоне.
  - Цвета палитры (`Light`, `Midlight`, `Dark`, `Mid`, `Shadow`) настроены корректно.
  - Исправлен цвет `HighlightedText` (теперь белый).
- **Светлая тема**:
  - Чёрный текст на светлых фонах с аккуратными границами.
- Эффекты при наведении и нажатии добавлены для обоих тем.

5. Улучшения отображения блоков кода
- Устранены большие отступы:
  - `margin: 20px 0` → `margin: 0`
  - `padding: 15px` → `padding: 10px`
- Фоны блоков кода теперь гармонируют с темой:
  - **Тёмная тема**: `#1a1a1a` (фон), `#e0e0e0` (текст), inline-код — розовый (`#ff9ea8`).
  - **Светлая тема**: `#f0f0f0` (фон).
- Добавлено `!important` ко всем стилям тёмной темы, чтобы избежать перезаписи



v0.0.6
--------
1. Добавлены новые функции для правильного парсинга:
   - find_matching_brace() - находит соответствующую закрывающую скобку с учетом вложенности
   - extract_latex_commands() - извлекает LaTeX команды с правильной обработкой всех аргументов

2. Улучшена обработка сложных конструкций:
   - Теперь корректно обрабатываются вложенные фигурные скобки
   - Поддержка опциональных аргументов в квадратных скобках [...]
   - Правильная обработка команд с несколькими аргументами

3. Создан файл LATEX_RULES.md с четкими правилами:
   - Когда можно использовать команды без обрамления
   - Когда обязательно нужно $...$
   - Примеры правильного и неправильного использования



v0.0.5
--------
1. Добавлена поддержка всех вариантов LaTeX-синтаксиса:
   - \[ ... \] для display формул (стандартный LaTeX)
   - \( ... \) для inline формул (стандартный LaTeX)
   - Одиночные LaTeX команды без обрамления: \text{см}, \frac{20}, \vec{a}, \implies, \cdot, \uparrow, \overrightarrow{AB}, \boxed{...} и т.д.

2. Расширена конфигурация MathJax:
   - Добавлены все математические пакеты (ams, color, cancel, mhchem и др.)
   - Настроены макросы для \implies и \iff
   - Улучшена обработка HTML-тегов

3. Улучшена предварительная обработка:
   - Регулярные выражения теперь распознают более 20 типов LaTeX команд
   - Все LaTeX команды защищаются от обработки Markdown
   - Поддержка вложенных конструкций

4. Созданы тестовые файлы:
   - test_latex_extended.md - с примерами всех вариантов формул
   - README_RU.md - документация по использованию на русском



v0.0.4
--------
Что было исправлено:
1. Добавлена предварительная обработка LaTeX формул:
   - Формулы защищаются от обработки Markdown с помощью placeholder'ов
   - Display формулы: $$...$$ сохраняются как $$$...$$$ для MathJax
   - Inline формулы: $...$ остаются как $...$

2. Исправлена конфигурация MathJax:
   - inlineMath: [['$', '$']] - для inline формул
   - displayMath: [['$$$', '$$$']] - для display формул
   - Убраны конфликтующие настройки

3. Упрощен HTML-шаблон:
   - Убрано двойное экранирование фигурных скобок
   - MathJax загружается асинхронно
   - Конфигурация вынесена в отдельный блок перед загрузкой библиотеки



v0.0.3
--------
============================================
ОПИСАНИЕ ВСЕХ ФУНКЦИЙ И ВНЕСЕННЫХ ИЗМЕНЕНИЙ
============================================

1. ИСПРАВЛЕНИЕ ПРОБЛЕМЫ С КОДИРОВКОЙ:
   - Добавлено меню "Encoding" с выбором популярных кодировок
   - Реализована ручная смена кодировки через меню
   - При переключении кодировки файл автоматически перечитывается
   - Сохраняется позиция курсора и прокрутки при смене кодировки
   - Добавлена статусная метка, отображающая текущую кодировку
   - Поддержка основных кодировок для кириллицы: UTF-8, Windows-1251, KOI8-R и др.
   - Сохранение выбранной кодировки в настройках приложения

2. РЕАЛИЗАЦИЯ ТЕМНОЙ ТЕМЫ:
   - Добавлен переключатель "Dark Mode" в меню View (Ctrl+D)
   - Полная настройка цветовой схемы для редактора и превью
   - Автоматическое обновление подсветки синтаксиса при переключении темы
   - Сохранение состояния темы между сессиями через QSettings
   - Специальные цвета для темной темы во всех элементах интерфейса
   - Темные стили для превью Markdown (заголовки, код, цитаты и т.д.)

3. УЛУЧШЕНИЕ ПОДСВЕТКИ СИНТАКСИСА:
   - Полная реализация подсветки кода
   - Добавлено расширение codehilite для правильной обработки блоков кода
   - Реализована детальная цветовая схема для всех элементов кода:
     * Комментарии, ключевые слова, строки, числа и т.д.
     * Поддержка различных языков программирования
   - Автоматическое применение темных стилей в темной теме
   - Улучшенная подсветка для Markdown-синтаксиса (заголовки, жирный, курсив и т.д.)

4. ИСПРАВЛЕНИЕ ОШИБОК И ПРЕДУПРЕЖДЕНИЙ:
   - Добавлено подавление предупреждений DeprecationWarning в начале скрипта
   - Исправлены ошибки с экранированием фигурных скобок в HTML-шаблоне
   - Улучшена обработка ошибок при открытии и сохранении файлов
   - Добавлена обработка ошибок кодировки с пользовательскими сообщениями
   - Исправлен импорт QSettings (теперь из PyQt5.QtCore)

5. ДОПОЛНИТЕЛЬНЫЕ УЛУЧШЕНИЯ:
   - Сохранение настроек приложения между запусками (тема, кодировка)
   - Улучшенный интерфейс с разделением на панели редактора и превью
   - Поддержка LaTeX-формул через MathJax в превью
   - Все горячие клавиши работают как в стандартных текстовых редакторах
   - Возможность скрыть панель превью для большего рабочего пространства
   - Статусная строка с информацией о текущих операциях

6. ТЕХНИЧЕСКИЕ УЛУЧШЕНИЯ:
   - Использование QSettings для хранения пользовательских настроек
   - Добавлен debounce-таймер для оптимизации обновления превью
   - Улучшенная обработка ошибок с пользовательскими сообщениями
   - Поддержка всех основных операций с файлами (новый, открыть, сохранить)
   - Автоматическое определение кодировки при открытии файлов



v 0.0.2
--------
Что изменилось:
- Улучшенное определение кодировки:
- Сначала проверяем наличие BOM (Byte Order Mark) для UTF-8
- Если уверенность chardet ниже 70% или определена ASCII, но файл содержит не-ASCII символы
- Автоматически пробуем распространённые кодировки для кириллицы:
   - cp1251 (Windows-1251) - основная кодировка для русского в Windows
   - koi8-r - альтернативная кодировка для кириллицы
   - utf-8 - как fallback
   - latin1 - для общего случая

Безопасное чтение:
- Если ни одна кодировка не подошла, используем errors='replace' чтобы избежать падения программы
- Проверяем первые 1000 байт на наличие не-ASCII символов